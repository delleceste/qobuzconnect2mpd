# Meson build directory
/builddir/

# Real config with credentials — run-from-repo, copied from the tracked
# conf/qobuzconnect2mpd.conf.example template. Never commit secrets.
/conf/qobuzconnect2mpd.conf
